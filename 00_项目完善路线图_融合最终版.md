# common_agent 项目完善路线图（融合最终版）

日期：2026-09-22  
适用项目：`C:\Users\75662\Desktop\agent_projects\common_agent`

参考材料：已经完成的 LangGraph 与 mini-agent 练习、当前 common_agent 代码、`agent_source_reading\04_最终版改进计划.md`。原 `00_项目完善路线图.md` 已并入本文并按面试目标重新排序，原文件已删除。

> M1–M10 是大阶段；`M1.1`、`M1.2` 这类编号是可以独立实现、独立解释、独立提交的最小子项。总表与下文使用同一编号，不再出现“上面一套、下面一套”的重复排序。

## 一、最终目标

把 common_agent 做成一个面试时能完整讲清楚的通用 Agent 应用：

- 能从用户请求讲到模型决策、工具调用、结果回灌和最终回答；
- 能说明工具契约、参数校验、权限、提示注入和错误恢复；
- 能演示 RAG、MCP、checkpoint、人工确认和服务化边界；
- 每个能力都有真实代码、运行结果和清晰取舍；
- 不为了“像大系统”堆砌个人难以维护和解释的多 Agent 或基础设施。

项目追求可解释、可控、可演示，功能数量不作为完成标准。

## 二、当前基线

| 能力 | 当前实现 | 状态 |
|---|---|---|
| 模型接入 | OpenAI 兼容协议、千问配置、客户端 60 秒超时和重试 | 已有 |
| 手写 Agent loop | `model → tools → model`，最大轮数、连续重复保护、跨消息重置 | 已完成 M1.1–M1.3 |
| 文件工具 | workspace 路径边界、文本限制、大小限制、禁止覆盖 | 已有基础版，待纳入统一 M2 边界 |
| RAG | 切块、Embedding、SQLite、余弦召回、source 返回 | 教学版，待 M5 产品化 |
| MCP | stdio Server、动态发现 Schema、异步工具桥接 | 最小版，待 M6 治理 |
| 会话 | LangGraph SQLite checkpoint、`thread_id` 恢复 | 已有，待 M4 补轨迹 |
| CLI | 多轮对话、工具轨迹显示 | 已有，M8 再服务化 |

## 三、排序原则

按下面五个条件排序：

1. Agent 开发面试中的通用性；
2. 对项目正确性和安全性的影响；
3. 能否用代码与运行结果证明；
4. 能否清楚说明设计取舍；
5. 实现成本和回退难度。

热门概念只在前四项接近时作为排序参考。多 Agent、A2A、自我改进等能力放到单 Agent 基础稳定之后。

## 四、面试优先级总表

下面的 40 项是跨章节的真正实施顺序。排序首先服务于面试故事：先讲清 Agent 如何运行，再讲工具为何安全、失败如何恢复、上下文如何留痕，随后接 RAG/MCP 和产品化，最后才讲子 Agent 与评测。

### 4.1 40 项全局实施顺序

| 顺序 | 子项 | 这一项在面试故事中的作用 | 状态 |
|---:|---|---|---|
| 1 | M1.1 模型—工具—模型闭环 | 先证明 Agent 不是一次模型调用，而是决策与工具回环 | ✅ 已完成 |
| 2 | M1.2 停止条件 | 解释循环如何正常结束、如何防止无限运行 | ✅ 已完成 |
| 3 | M1.3 运行边界重置 | 解释 checkpoint 为什么不能污染下一次用户请求 | ✅ 已完成 |
| 4 | M2.1 工具契约与轻量登记表 | 在现有 LangChain Tool 之上登记来源、风险和可用范围 | ✅ 已完成（与 M2.2 联合） |
| 5 | M2.2 工具权限分级 | 采用 allow/ask/deny 三档；参数校验是权限之上的安全要求，不另算一档 | ✅ 已完成（与 M2.1 联合） |
| 6 | M2.3 执行前校验 | 把路径、参数、覆盖和结果大小安全落到执行层 | 🟡 已有路径基础，待统一 |
| 7 | M3.1 结构化输出 | 让下游代码只接受可校验的数据结构 | ⬜ 待做 |
| 8 | M3.2 错误分类 | 说明为什么用户错误、模型错误和系统错误不能一律重试 | ⬜ 待做 |
| 9 | M3.3 有限恢复 | 展示错误回喂、有限重试和安全结束的边界 | ⬜ 待做 |
| 10 | M3.4 正确收尾 | 保证失败时明确告知用户，不伪装成成功 | ⬜ 待做 |
| 11 | M2.4 提示注入边界 | 在已有工具安全故事上补充外部内容不能获得指令权限 | ⬜ 待做 |
| 12 | M4.1 State / messages / checkpoint 边界 | 解释运行态、上下文和恢复点各自负责什么 | 🟡 checkpoint 已有 |
| 13 | M4.3 轨迹账本 | 让每次模型决策和工具调用都能被解释、定位和复现 | ⬜ 待做 |
| 14 | M4.2 上下文预算 | 解决多轮工具调用让上下文无限增长的问题 | ⬜ 待做 |
| 15 | M4.4 恢复与回放事实 | 把持久化从“存数据”提升为“能恢复一次运行” | 🟡 checkpoint 已有，回放待做 |
| 16 | M5.1 增量 ingestion | 说明知识库内容如何进入系统并增量更新 | ⬜ 待做 |
| 17 | M5.2 双路召回 | 同时处理精确术语和语义相似问题 | ⬜ 待做 |
| 18 | M5.3 合并与重排 | 让召回结果经过统一排序，而不是直接相信单一路径 | ⬜ 待做 |
| 19 | M5.4 证据对象 | 让回答能够追溯 source、chunk、分数和版本 | 🟡 source 已有，待规范化 |
| 20 | M5.5 无证据拒答 | 让 RAG 具备“证据不足就拒答”的边界 | ⬜ 待做 |
| 21 | M6.1 MCP 生命周期 | 说明外部工具 Server 如何启动、发现、调用和关闭 | 🟡 最小版已有 |
| 22 | M6.2 动态工具过滤 | 证明 MCP 工具也必须经过项目自己的权限边界 | ⬜ 待做 |
| 23 | M6.3 结果三态 | 区分成功、业务错误和传输/超时错误 | ⬜ 待做 |
| 24 | M6.4 异步边界 | 解释为什么 SDK 是 async，但当前工具仍按顺序执行 | 🟡 异步桥接已有 |
| 25 | M7.1 高风险人工确认 | 让写入、发送、删除等副作用进入 HITL 故事 | ✅ 基础版已完成；高级恢复待补 |
| 26 | M7.2 取消与工具超时 | 处理外部工具真正卡住时的停止和取消 | ⬜ 待做 |
| 27 | M7.3 幂等与副作用 | 说明重试为什么不能重复写入或发送 | ⬜ 待做 |
| 28 | M7.4 持久化先后 | 说明中断时如何保存意图、结果和恢复顺序 | ⬜ 待做 |
| 29 | M8.1 流式事件 | 把内部执行过程变成用户和服务可以观察的事件 | ⬜ 待做 |
| 30 | M8.2 FastAPI 接口 | 把 CLI 原型变成可以演示的 Agent 服务 | ⬜ 待做 |
| 31 | M8.3 会话与并发隔离 | 说明多用户服务中的 thread 和资源边界 | ⬜ 待做 |
| 32 | M8.4 Docker 与部署说明 | 让面试官可以复现启动和验证项目 | ⬜ 待做 |
| 33 | M9.1 工具按需加载 | 工具多到影响上下文时再引入动态加载 | ⬜ 后置 |
| 34 | M9.2 受控规划 | 证明什么时候需要计划节点，什么时候不需要 | ⬜ 后置 |
| 35 | M9.3 子 Agent 隔离 | 先证明单 Agent 不够，再讨论拆分 | ⬜ 后置 |
| 36 | M9.4 受控并行与汇总 | 说明 gather 只适用于独立、可取消的任务 | ⬜ 后置 |
| 37 | M10.1 评测数据集 | 为稳定的产品行为建立可重复输入 | ⬜ 最后 |
| 38 | M10.2 轨迹与结果断言 | 检查工具、参数、路径和最终答案，而不是只看文本 | ⬜ 最后 |
| 39 | M10.3 安全回归与回放 | 防止后续版本破坏越权、注入和拒答边界 | ⬜ 最后 |
| 40 | M10.4 成本与延迟 | 最后量化 token、耗时、重试和工具成本 | ⬜ 最后 |

### 4.2 按 M 阶段索引

下面这张表只用于按主题回看，不代表实际执行顺序；实际顺序以上面的 4.1 为准。

| 编号 | 面试优先级 | 子项 | 具体交付 | 面试价值 | 状态 |
|---|---:|---|---|---|---|
| M1.1 | 最高 | 模型—工具—模型闭环 | 手写 `model → tool_call → handler → tool_result → model` | 能解释 Agent 与普通问答的区别 | ✅ 已完成 |
| M1.2 | 最高 | 停止条件 | 最大工具轮数、连续相同调用安全出口 | 能回答“循环什么时候停” | ✅ 已完成 |
| M1.3 | 最高 | 运行边界重置 | 新用户消息重置本轮计数、签名和停止原因 | 能解释 checkpoint 不污染下一轮 | ✅ 已完成 |
| M2.1 | 很高 | 工具契约与轻量登记表 | 在现有 LangChain Tool 之上登记来源、风险和可用范围；不重写 LangGraph | 能解释模型如何发现工具、代码如何分发，以及项目自己的治理信息在哪里 | ✅ 已完成（与 M2.2 联合） |
| M2.2 | 很高 | 工具权限分级 | 采用 allow、ask、deny 三档；参数校验作为独立安全要求叠加 | 能回答“模型会不会越权，以及为什么要人工确认” | ✅ 已完成（与 M2.1 联合） |
| M2.3 | 很高 | 执行前校验 | 路径、覆盖、参数、结果大小在 handler 前校验 | 把安全落到执行层 | 🟡 已有路径基础，待统一 |
| M2.4 | 很高 | 提示注入边界 | RAG、网页、MCP 返回值只当数据，不获得指令权限 | 能解释提示词安全的局限 | ⬜ 待做 |
| M3.1 | 很高 | 结构化输出 | JSON Schema/Pydantic 校验路由和工具参数 | 能解释自由文本为何不能直接驱动代码 | ⬜ 待做 |
| M3.2 | 很高 | 错误分类 | 区分用户可修复、模型可修复、系统不可修复 | 能说明为什么不能一律重试 | ⬜ 待做 |
| M3.3 | 很高 | 有限恢复 | 可修复错误有限回喂，系统错误安全结束 | 能讲清恢复路径与代价 | ⬜ 待做 |
| M3.4 | 很高 | 正确收尾 | 保险丝和不可恢复错误给出明确未完成说明 | 避免把失败伪装成成功 | ⬜ 待做 |
| M4.1 | 很高 | State / messages / checkpoint 边界 | 明确运行态、上下文、恢复点的职责 | 解释多轮状态设计 | 🟡 checkpoint 已有 |
| M4.2 | 很高 | 上下文预算 | 选择、压缩消息，tool_call 与 ToolMessage 成对保留 | 解决上下文无限增长 | ⬜ 待做 |
| M4.3 | 很高 | 轨迹账本 | 记录轮次、模型决策、工具、参数摘要、结果、耗时和错误 | 出错时可以复现，不靠猜 | ⬜ 待做 |
| M4.4 | 很高 | 恢复与回放事实 | 能从 checkpoint 和轨迹说明一次运行如何恢复 | 把持久化讲成工程能力 | 🟡 checkpoint 已有，回放待做 |
| M5.1 | 高 | 增量 ingestion | 解析、切块、哈希、增量更新 | 说明知识库如何维护 | ⬜ 待做 |
| M5.2 | 高 | 双路召回 | 关键词与向量召回并存 | 解决精确词和语义词的差异 | ⬜ 待做 |
| M5.3 | 高 | 合并与重排 | fusion/rerank 保留分数和版本 | 提升检索稳定性 | ⬜ 待做 |
| M5.4 | 高 | 证据对象 | 返回 source、chunk、分数和版本 | 回答能够追溯依据 | 🟡 source 已有，待规范化 |
| M5.5 | 高 | 无证据拒答 | 证据不足时阻止模型编造 | 能解释 RAG 不是“搜到就回答” | ⬜ 待做 |
| M6.1 | 高 | MCP 生命周期 | Client 启动、发现、调用、关闭 Server | 理解协议边界 | 🟡 最小版已有 |
| M6.2 | 高 | 动态工具过滤 | Schema 转换后按权限交给模型 | MCP 工具不绕过安全层 | ⬜ 待做 |
| M6.3 | 高 | 结果三态 | 成功、业务错误、超时/传输错误分开 | 能解释外部工具失败 | ⬜ 待做 |
| M6.4 | 高 | 异步边界 | 保留 SDK 的 async 接口；串行 await 不冒充并行 | 能准确解释 async 的真实原因 | 🟡 异步桥接已有 |
| M7.1 | 中高 | 高风险人工确认 | `interrupt` 暂停，`Command(resume=...)` 审批后继续执行 | 能说明 HITL 的必要性 | ✅ 基础版已完成 |
| M7.2 | 中高 | 取消与工具超时 | 工具侧 `wait_for`、取消传播和安全结束 | 处理真正会卡住的外部工具 | ⬜ 待做 |
| M7.3 | 中高 | 幂等与副作用 | 重试不重复写入或发送 | 说明 Agent 的副作用治理 | ⬜ 待做 |
| M7.4 | 中高 | 持久化先后 | 先记录意图，再记录结果，支持恢复 | 能讲清故障恢复顺序 | ⬜ 待做 |
| M8.1 | 中高 | 流式事件 | 将模型、工具、结果变成可观察事件 | 改善真实使用体验 | ⬜ 待做 |
| M8.2 | 中高 | FastAPI 接口 | 将 CLI 能力暴露为服务 | 具备岗位作品的服务入口 | ⬜ 待做 |
| M8.3 | 中高 | 会话与并发隔离 | thread、用户和资源边界清晰 | 说明服务化后的状态管理 | ⬜ 待做 |
| M8.4 | 中高 | Docker 与部署说明 | 可复现启动、配置和健康检查 | 方便面试现场展示 | ⬜ 待做 |
| M9.1 | 中 | 工具按需加载 | 工具数量真的超过上下文承受范围时再加载 | 能讲清为什么不把所有工具都塞给模型 | ⬜ 后置 |
| M9.2 | 中 | 受控规划 | 任务确实需要计划时才增加计划节点 | 区分工作流和自由规划 | ⬜ 后置 |
| M9.3 | 中 | 子 Agent 隔离 | 只有上下文或职责确实需要时才拆分 | 避免为了多智能体而多智能体 | ⬜ 后置 |
| M9.4 | 中 | 受控并行与汇总 | 仅并行独立、可取消的任务，保留来源和失败 | 能说明 gather 的适用边界 | ⬜ 后置 |
| M10.1 | 最后 | 评测数据集 | 为稳定任务建立输入、期望行为和安全样本 | 有可重复的质量基线 | ⬜ 最后 |
| M10.2 | 最后 | 轨迹与结果断言 | 检查工具、参数、路径和最终答案 | 评测不是只看一句最终文本 | ⬜ 最后 |
| M10.3 | 最后 | 安全回归与回放 | 越权、注入、拒答、恢复路径可重复 | 防止后续迭代破坏安全 | ⬜ 最后 |
| M10.4 | 最后 | 成本与延迟 | 统计 token、耗时、重试和工具成本 | 能回答工程取舍 | ⬜ 最后 |

## 五、每个子项的交付规则

每个 `Mx.y` 都按同一个闭环完成：

```text
明确问题 → 设计最小接口 → 实现一个子项 → 真实运行 → 更新 README → 由 WorkBuddy 提交
```

每个子项的说明必须回答：解决什么问题、入口和状态在哪、失败走哪条路、为什么这样设计、明确没做什么。

## 六、阶段说明

### M1：手写主循环与运行边界

#### M1.1 模型—工具—模型闭环

当前已完成。模型决定是否调用工具，代码负责执行工具并把结果重新交给模型。

#### M1.2 停止条件

当前已完成。最大工具轮数防止无限循环，连续相同工具请求防止原地转圈。

#### M1.3 运行边界重置

当前已完成。新用户消息会清零本轮计数、重复签名和停止原因，避免 checkpoint 污染下一次回答。

### M2：工具契约与安全执行

#### M2.1 工具契约与轻量登记表

已完成，见 `app/tool_registry.py`。这里不重写 LangGraph 或 LangChain：框架继续提供 Tool 对象、参数 Schema、`bind_tools` 和 `ainvoke`；项目层新增 `ToolPolicy` 和 `ToolRegistry`，登记工具来源、权限模式、风险、参数校验要求和可用作用域。`app/agent.py` 先建立 Registry，再把当前作用域允许暴露的工具交给主循环；`manual_loop.py` 使用同一份 Registry 建立执行查找表，避免模型说明书和实际 handler 来自两套名单。

M2.1 与 M2.2 联合交付。权限采用三档 `allow / ask / deny`，并已接入工具节点分发：`allow` 才进入 `tool.ainvoke`，`ask` 通过 LangGraph `interrupt` 暂停，调用方用 `Command(resume=...)` 明确批准后才执行，`deny` 直接拒绝；“是否需要参数校验”不是第四档，而是叠加在权限之上的安全要求。M7.1 的基础 HITL 链路已完成，后续只补更复杂的审批恢复和可靠性。

#### M2.2 工具权限分级

已完成基础策略登记。权限分级没有一个必须照搬的行业标准；本项目采用更容易解释的三档执行决策：`allow` 自动执行、`ask` 通过 `interrupt` 等待人工确认、`deny` 不暴露给当前 Agent。路径校验、文件类型校验和结果大小限制属于参数安全，不另造“第四种权限”。当前 `save_new_text_file` 与 `index_knowledge_base` 登记为 `ask`，只读文件工具和知识检索登记为 `allow`。

#### M2.3 执行前校验

已有路径基础，待统一。路径、覆盖、参数和结果大小都必须在 handler 执行前校验。

#### M2.4 提示注入边界

待做。RAG、网页和 MCP 返回值只作为数据，不能获得系统指令级别的权限。

### M3：结构化输出与错误恢复

#### M3.1 结构化输出

待做。下游代码只接受经过 JSON Schema/Pydantic 校验的路由、工具参数和关键业务结果。

#### M3.2 错误分类

待做。区分用户可修复、模型可修复和系统不可修复错误，避免所有异常都走同一种重试。

#### M3.3 有限恢复

待做。可修复错误有限回喂模型，系统错误安全结束并保留原因。

#### M3.4 正确收尾

待做。保险丝和不可恢复错误都要产生明确的未完成说明，不能伪装成正常回答。

### M4：状态、上下文与轨迹

#### M4.1 State、messages、checkpoint 边界

checkpoint 已有。State 是一次运行的共享白板，messages 是模型上下文，checkpoint 是恢复点。

#### M4.2 上下文预算

待做。选择和压缩消息时，不能拆开 assistant 的 tool_call 与对应 ToolMessage。

#### M4.3 轨迹账本

待做。记录轮次、模型决策、工具、参数摘要、结果、耗时、异常和停止原因，先记录事实，不急着评分。

#### M4.4 恢复与回放事实

checkpoint 已有，回放待做。能够从 checkpoint 和轨迹说明一次运行如何恢复。

### M5：RAG 证据闭环

#### M5.1 增量 ingestion

待做。解析、切块、哈希和增量更新构成知识库的输入层。

#### M5.2 双路召回

待做。关键词召回负责精确术语，向量召回负责语义相似，两路结果再合并。

#### M5.3 合并与重排

待做。统一分数和版本，减少单一召回方式的偏差。

#### M5.4 证据对象

已有 source 基础，待规范化。每条证据携带 source、chunk、分数和版本。

#### M5.5 无证据拒答

待做。证据不足时阻止模型编造，明确说明当前资料不支持结论。

### M6：MCP 与外部工具

#### M6.1 MCP 生命周期

最小版已有。Client 负责启动、发现、调用和关闭 Server；MCP 不负责 Agent 决策循环。

#### M6.2 动态工具过滤

待做。Schema 转换和权限过滤后，才把 MCP 工具交给模型。

#### M6.3 结果三态

待做。统一区分成功、业务错误和超时/传输错误，不把外部异常伪装成正常结果。

#### M6.4 异步边界

异步桥接已有。官方 Python SDK 的 stdio Client 是异步接口，因此保留 async/await；串行 await 是协议适配，不等于已经实现并行。

### M7：HITL 与可靠性

#### M7.1 高风险人工确认

基础版已完成。`execute_tools_node` 在 ask 工具真正执行前调用 LangGraph `interrupt`，把工具名和参数交给调用方；CLI 展示请求，用户确认后用同一 `thread_id` 发送 `Command(resume={"approved": True})`，节点恢复后才进入 `tool.ainvoke`。拒绝则恢复为 False，工具不会产生副作用。后续可再补多级审批、审批超时和跨进程恢复。

#### M7.2 取消与工具超时

待做。为工具侧加入 `asyncio.wait_for`、取消传播和安全结束；模型请求已有 `app/agent.py` 的 60 秒超时。

#### M7.3 幂等与副作用

待做。重试不能重复写入、发送或删除，必须使用幂等键或可验证的写入规则。

#### M7.4 持久化先后

待做。先记录工具意图，再记录执行结果，保证中断后能解释和恢复。

### M8：流式输出与服务化

#### M8.1 流式事件

待做。把模型、工具请求和工具结果转换为调用方可观察的事件。

#### M8.2 FastAPI 接口

待做。将 CLI 能力暴露为服务入口。

#### M8.3 会话与并发隔离

待做。保证 thread、用户和资源边界清晰。

#### M8.4 Docker 与部署说明

待做。提供可复现的启动、配置和健康检查说明。

### M9：受控规划、按需加载与子 Agent

#### M9.1 工具按需加载

后置。只有工具数量真的超过上下文承受范围时才引入。

#### M9.2 受控规划

后置。任务确实需要计划时才增加计划节点，区分固定工作流和自由规划。

#### M9.3 子 Agent 隔离

后置。只有上下文或职责确实需要时才拆分，先证明单 Agent 不够。

#### M9.4 受控并行与汇总

后置。只并行独立且可取消的任务，汇总时保留来源、失败和取消状态。

### M10：评测、回放与成本分析

#### M10.1 评测数据集

最后实施。为稳定任务建立输入、期望行为和安全样本。

#### M10.2 轨迹与结果断言

最后实施。检查工具、参数、路径、恢复路径和最终答案，不能只看一句最终文本。

#### M10.3 安全回归与回放

最后实施。让越权、注入、拒答和恢复路径可以重复验证。

#### M10.4 成本与延迟

最后实施。统计 token、耗时、重试次数和工具成本。

M4 只记录事实，M10 才对事实打分；评测目标、轨迹、结果、安全和成本都必须可重复。

## 七、最终验收口径

达到简历版本时，现场演示应能完成：

```text
用户提出任务
  → Agent 判断是否需要工具
  → Schema、权限和路径边界校验
  → 失败按类别恢复或安全结束
  → 高风险动作触发人工确认
  → RAG/MCP 结果带 source 或状态回灌
  → Agent 继续或停止
  → 会话和轨迹可恢复、可解释
```

届时简历可以写：

> 独立开发一个可恢复、可审计的通用 Agent 内核，手写实现模型工具循环，接入受限文件工具、RAG 和 MCP，并补充结构化输出校验、错误分类、上下文预算、权限分级、人工确认、SQLite 会话持久化和服务化接口。

简历中的每个名词，都必须能在代码、运行结果和某个 `Mx.y` 子项的提交记录中找到证据。
