# common_agent 项目完善路线图（融合最终版）

日期：2026-09-21  
适用项目：`C:\Users\75662\Desktop\agent_projects\common_agent`  
参考材料：原 `00_项目完善路线图.md`（内容已并入本文，原文件已删除）、`agent_source_reading\04_最终版改进计划.md`（已归档，仅作追溯）、已经完成的 LangGraph 与 mini-agent 练习。

> 仓库里只保留本文这一份计划。`04_最终版改进计划.md` 是同一天写下的更啰嗦的旧版，别再对着它排期。

> 这是一份开发顺序，不是功能清单。每完成一个阶段，都要能运行、能解释、能说明代价，再进入下一阶段。

## 一、最终目标

把 `common_agent` 做成一个面试时能完整讲清楚的通用 Agent 应用：

- 能解释模型、工具、状态、上下文和持久化之间的关系；
- 能说明工具为什么会被调用、参数如何校验、错误如何处理；
- 能演示 RAG、MCP、记忆和人工确认，但不把所有能力堆成一个无法维护的巨型系统；
- 每个关键功能都有明确边界：为什么做、解决什么问题、付出什么代价、暂时没做什么；
- 最终能从真实代码、运行轨迹和 Git commit 讲出完整工程故事。

项目追求的是“可解释、可控、可演示”，不是功能数量最多。

## 二、当前基线与真正缺口

当前已经具备：

| 能力 | 当前实现 | 判断 |
|---|---|---|
| 模型接入 | OpenAI 兼容协议、千问配置 | 已有 |
| 文件工具 | workspace 路径边界、文本类型限制、大小限制、禁止覆盖 | 已有 |
| RAG | 切块、Embedding、SQLite 保存、余弦召回、来源返回 | 已有教学版 |
| MCP | stdio Server、动态发现 Schema、桥接为 LangChain Tool | 已有最小版 |
| 会话 | LangGraph SQLite checkpoint、`thread_id` 恢复 | 已有 |
| CLI | 多轮对话、工具轨迹显示 | 已有 |

真正的缺口按严重程度排序：

1. 目前只有轮数上限，缺少重复调用检测、预算和单步超时，Agent 仍可能原地打转。
2. 工具权限还不够细，读、写、外部请求和高风险动作需要不同治理方式。
3. 没有稳定的结构化输出、错误分类和恢复路径。
4. 上下文会增长，但还没有预算、压缩和消息成对保留规则。
5. 没有统一的轨迹记录，出错时只能凭猜测复现。

主循环本身不再是缺口：`app/manual_loop.py` 已经把循环显式写了出来，后面的每一项都是在它上面加边界，而不是换掉它。

## 三、功能排序原则

一个功能进入主线，需要同时满足：

1. 多数成熟 Agent 都需要；
2. 能解决当前项目的真实缺口；
3. 能用代码证明，而不是只写在提示词里；
4. 能解释代价和边界；
5. 能独立提交，失败时容易回退。

“行业很热门”只能作为排序的第二依据。单 Agent 主循环、工具安全、上下文和错误处理完成之前，不因为多 Agent、A2A 或自动进化很热门就提前加入。

## 四、最终优先级总表

### 第一梯队：完成后项目就具备面试核心竞争力

| 顺序 | 能力 | 为什么现在做 | 学过的素材 | 完成标志 | 状态 |
|---|---|---|---|---|---|
| 1 | 手写 Agent 主循环 | Agent 的定义就是模型决策与工具回环 | `mini-agent\exercises\04_agent_loop.py`、`gpt_agent_learning\07_第一个工具循环.py` | 不依赖框架自动循环也能完成“模型 → tool_call → handler → tool_result → 下一轮” | ✅ 已完成：`app/manual_loop.py` |
| 2 | 防转圈三件套 | 轮数上限单独使用不够 | `gpt_agent_learning\04_循环与保险丝.py` | 轮数上限、同工具同参数重复检测、单步超时/预算都能触发安全出口 | ⬜ 待做 |
| 3 | 工具 Registry、权限分级与调用账本 | Schema、handler、权限和审计需要统一入口 | `mini-agent\exercises\15_guardrails.py`、现有 `app/workspace_tools.py` | 工具声明只读/可写/需审批/禁止，统一记录调用 ID、参数摘要和结果摘要 | ⬜ 待做 |
| 4 | 提示注入防御 | 工具结果、网页和 RAG 内容都可能携带伪指令 | `mini-agent\exercises\15_guardrails.py` | 外部内容被当作数据，执行层权限校验不会被模型文字绕过 | ⬜ 待做 |
| 5 | 结构化输出与代码校验 | 下游代码不能依赖模型自由文本 | `mini-agent\exercises\10_structured_output.py`、`gpt_agent_learning\14_结构化输出与安全路由.py` | Pydantic/JSON Schema 校验失败后，能把明确错误回给模型或安全结束 | ⬜ 待做 |
| 6 | 错误分类与分级恢复 | 业务错误、参数错误和系统故障不能一律重试 | `mini-agent\exercises\12_error_handling.py`、`gpt_agent_learning\09_错误分类与安全降级.py` | 能分别处理“用户可修复”“模型可修复”“系统不可修复”三类错误 | ⬜ 待做 |
| 7 | 上下文预算与压缩 | 多轮工具调用会让 prompt 无限制增长 | `mini-agent\exercises\13_context_engineering.py`、`gpt_agent_learning\11_上下文压缩.py` | 预算超限时压缩中间历史；assistant tool_call 和对应 tool_result 永远成对保留 | ⬜ 待做 |
| 8 | 轨迹记录 | 后续所有调试和优化都需要事实依据 | `mini-agent\exercises\16_eval_observability.py` 中的观测部分 | 每轮记录模型决策、工具、参数摘要、结果摘要、耗时和错误；不提前做评分 | ⬜ 待做 |

第一梯队完成后，项目已经足以支撑一次完整面试：可以从一次请求讲到模型、工具、错误、上下文和轨迹。

### 第二梯队：把核心能力做成真正可用的产品

| 顺序 | 能力 | 主要价值 | 学过的素材 |
|---|---|---|---|
| 9 | 高风险动作人工确认 | 写入、外部请求和敏感操作不能只靠模型自觉 | `mini-agent\exercises\15_guardrails.py`、`gpt_agent_learning\10_人工确认与恢复.py` |
| 10 | RAG 检索质量升级 | 纯向量检索对型号、缩写和精确术语不稳定 | `mini-agent\exercises\17_rag_advanced.py` |
| 11 | 证据闸门与无证据拒答 | 把“检索到什么”和“能否回答”分开 | `gpt_agent_learning\15_RAG_最小证据闭环.py`、`16_自适应RAG与子图.py` |
| 12 | 长期记忆分层 | State、短期上下文、checkpoint、长期记忆不能混成一件事 | `gpt_agent_learning\12_持久化checkpoint与记忆边界.py` |
| 13 | MCP 结果三态 | 外部工具结果要区分成功、业务错误、超时 | `gpt_agent_learning\18_MCP接入图的工程边界.py`、现有 `app/mcp_bridge.py` |
| 14 | 可靠性增强 | 超时、取消、幂等、持久化先后顺序决定系统能否恢复 | `gpt_agent_learning\08_工具失败不是崩溃.py` |

### 第三梯队：有余力再做的增强能力

| 顺序 | 能力 | 何时值得做 | 取舍 |
|---|---|---|---|
| 15 | 流式输出 | CLI 或 Web 体验需要改善时 | 体验明显变好，但本身不是 Agent 决策能力 |
| 16 | FastAPI、SSE/WebSocket、Docker | 需要把作品作为服务展示时 | 有助于简历完整度，但不应阻塞核心主循环 |
| 17 | 工具按需加载 | 工具数量超过约 20 个时 | 工具少时增加复杂度没有收益 |
| 18 | RAG 子图/独立检索流水线 | 检索逻辑出现多个分支时 | 提升工程边界，优先级低于证据闸门 |
| 19 | 主管与专家子 Agent | 单 Agent 已无法承担上下文隔离需求时 | 先证明为什么不能用单 Agent，再做隔离和汇总 |
| 20 | 动态并行 | 任务数量运行时才确定且确实能并发时 | 先解决副作用和取消，再考虑并行 |

## 五、每个阶段都必须完成的工程闭环

```text
明确问题 → 设计最小接口 → 实现一个切片 → 真实运行
    ↓            ↓              ↓            ↓
写清代价     保留旧入口       更新 README    独立 commit
```

每个 commit 只解决一个能力，提交信息统一采用：

```text
feat(loop): add manual tool calling loop - expose stop conditions
```

每个阶段的交付说明固定回答五个问题：

1. 这次解决了什么真实问题？
2. 哪个文件负责入口、状态和执行？
3. 失败时走哪条路径？
4. 为什么这样设计，代价是什么？
5. 还有哪些能力明确没做？

## 六、第一阶段 M1：手写主循环

`app/manual_loop.py` 已完成最小版本，当前默认引擎就是这条手写图：

```text
用户输入
  ↓
组装 messages + tools
  ↓
调用模型
  ├─ 没有 tool_calls → 返回最终文本
  └─ 有 tool_calls
        ↓
    校验工具名与 JSON 参数
        ↓
    Registry 找 handler
        ↓
    执行工具并生成 tool message
        ↓
    回灌 messages，进入下一轮
```

M1 已接入现有 workspace、RAG 和 MCP 工具；它们在主循环看来都是带有名称和 `ainvoke` 的工具对象，因此不需要在第一步分别重写三套循环。

M1 的停止条件必须显式写在代码里：

- 没有 tool_calls；
- 达到最大轮数；
- 未知工具或参数无法解析；
- 工具返回不可恢复错误；
- 用户主动取消；
- 预算或超时耗尽。

当前 `app/agent.py` 只负责组装模型、三类工具和 `build_agent_graph`；项目运行时不再依赖 `create_agent` 黑盒。框架版不做对照实验、不进主线：同一个仓库里维护两套执行引擎，只会让「循环什么时候停」这个问题重新变糊。

## 七、第二阶段 M2：防转圈、结构化输出与错误处理

这三项放在同一个阶段，因为它们共同决定一次 Agent 运行能否安全结束：

- 轮数上限：防止无限循环；
- 完全相同工具和参数的重复检测：防止换不换方法都耗尽轮数；
- 单步超时和总预算：防止某个工具或模型请求拖垮整轮；
- Pydantic/JSON Schema：保证路由和工具参数可解析；
- 错误分类：业务错误可以回喂模型，系统错误要安全结束并记录；
- 优雅收尾：工具预算耗尽后，允许模型基于已有证据给出明确的未完成说明。

核心取舍：自动重试会提高成功率，但也会掩盖真实故障、增加成本和延迟，所以必须有上限、分类和轨迹记录。

## 八、第三阶段 M3：工具权限、提示注入与人工确认

工具权限至少分成四级：

| 级别 | 例子 | 处理方式 |
|---|---|---|
| 只读 | 列目录、读文件、检索 | 可直接执行 |
| 受限写入 | 新建文件、写入草稿 | 路径和覆盖规则强校验 |
| 高风险 | 外部请求、发送、删除、执行命令 | 暂停并请求人工确认 |
| 禁止 | 越出 workspace、读取密钥、未授权系统操作 | 执行层直接拒绝 |

提示词只能表达意图，不能承担安全责任。真正的安全边界必须位于 handler 之前：路径解析、权限判断、参数校验、审批和审计都不能被模型文本跳过。

外部网页、MCP 返回值和 RAG 片段统一当作不可信数据；它们可以作为回答依据，但不能获得系统指令级别的权限。

## 九、第四阶段 M4：上下文、轨迹与记忆

先把四件事分开：

| 名称 | 解决的问题 | 生命周期 |
|---|---|---|
| State | 当前运行中各节点共享什么 | 一次运行 |
| messages | 模型下一轮需要看到什么 | 当前会话窗口 |
| checkpoint | 中断/重启后从哪里恢复 | 会话或任务 |
| long-term memory | 跨会话保留哪些稳定事实 | 长期、可删除 |

上下文处理遵循“写、选、压、隔”：写入必要事件，选择本轮需要的消息，超过预算就压缩，跨任务内容隔离。工具调用和工具结果必须成对保留，不能只删除其中一半。

轨迹记录只做事实记录：时间、轮次、请求 ID、工具名、参数摘要、结果摘要、耗时、异常和停止原因。它暂时不打分，评分系统放到最后。

## 十、第五阶段 M5：RAG 产品化

当前 SQLite 全量扫描版本适合作为教学基线，但下一步要把检索链路拆开：

1. ingestion：解析、切块、哈希、增量更新；
2. retrieval：关键词和向量双路召回；
3. fusion/rerank：合并排序和重排；
4. evidence：保留 source、chunk、分数和版本；
5. answer gate：没有足够证据就明确拒答。

知识库规模很小时可以继续用 SQLite。只有数据量和查询并发真的需要时，才迁移 pgvector 或 Qdrant；换数据库本身不是项目目标。

## 十一、第六阶段 M6：MCP 与外部工具

MCP 是工具和上下文的接入协议，不是 Agent 的决策循环。common_agent 要保持这条边界：

- Client 管 Server 生命周期；
- 启动时发现 tools/resources/prompts；
- Schema 转换后才把授权工具交给模型；
- 调用结果统一转成成功、业务错误、超时三态；
- MCP 工具同样要经过权限、超时、结果大小和审计；
- Server 崩溃时安全失败，不把异常伪装成正常结果。

## 十二、后置能力

以下能力暂不阻塞核心项目：

- A2A 协议；
- 自我改进和自我修改代码；
- 自动切换大小模型；
- 动态并行扇出；
- 复杂插件生态；
- 为几十 KB 知识库提前引入大型向量基础设施。

它们不是永远不做，而是等项目已经有稳定主循环、权限、上下文和错误处理后，再根据真实需求决定。

## 十三、评测放到最后

当前不建立评测数据集和评分框架。等 M1–M6 至少稳定后，再补：

- 单步：工具名、参数、结构化输出是否正确；
- 轨迹：是否经过正确的工具和恢复路径；
- 结果：最终答案是否满足任务；
- 安全：是否拒绝越权、注入和高风险未审批动作；
- 回放：同一版本能否重现关键行为；
- 成本：token、延迟、重试次数和工具耗时。

“记轨迹”和“做评测”是两件事：轨迹是事实账本，评测是对账本打分。前者属于核心工程，后者等产品行为稳定后再做。

## 十四、最终验收口径

项目达到简历版本，需要能现场完成下面这条演示：

```text
用户提出任务
  → Agent 判断是否需要工具
  → 工具参数经过 Schema 和权限校验
  → 高风险动作触发人工确认
  → 工具执行结果带 source/状态回灌
  → Agent 继续或停止
  → 会话和轨迹可恢复、可解释
```

届时简历可以写：

> 独立开发一个可恢复、可审计的通用 Agent 内核，手写实现模型工具循环，接入受限文件工具、RAG 和 MCP，并补充结构化输出校验、错误分类、上下文预算、权限分级、人工确认和 SQLite 会话持久化。

这句话里的每个名词，都必须能在代码、运行结果和 commit 中找到对应证据。
