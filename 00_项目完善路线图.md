# common_agent 项目完善路线图

> 目标：把当前的最小 Agent 内核，逐步做成一个能放进简历、能现场演示、每个关键设计都能自己解释的通用 Agent 应用。
>
> 当前策略：先把产品能力做完整，再做评测/eval。评测不是删除，而是明确后置；当前主线不阅读、不实现、不维护评测框架。

## 1. 项目定位

`common_agent` 不是“把所有开源 Agent 的功能都堆上去”，而是一个可控、可解释、可扩展的通用 Agent 基线：

```text
用户输入
  ↓
会话与上下文
  ↓
模型决策（直接回答 / 调工具 / 请求人工确认）
  ↓
工具安全边界与执行器
  ↓
工具结果、记忆、检索证据回灌
  ↓
下一轮决策或最终回答
```

面试时最终要能回答四件事：

1. 模型为什么会调用某个工具，工具说明书和真实 Python 函数如何关联。
2. 工具失败、参数错误、超时或需要审批时，系统如何安全结束或恢复。
3. 多轮会话、RAG、MCP、长期记忆和上下文压缩分别解决什么问题，边界在哪里。
4. 为什么系统没有把整台电脑和所有数据直接交给模型，权限、路径和副作用如何被限制。

## 2. 当前基线盘点

当前代码已经有一条可运行的最小闭环：

| 已有能力 | 入口 | 当前状态 |
| --- | --- | --- |
| 高层 Agent 循环 | `app/agent.py:47` 的 `create_agent` | 能运行，但循环主要由框架黑盒管理 |
| 工作区文件工具 | `app/workspace_tools.py` | 有路径逃逸拦截、类型限制、大小限制、禁止覆盖 |
| RAG | `app/rag_tools.py` | 有切块、Embedding、SQLite 保存和余弦召回；当前是教学型实现 |
| MCP | `app/mcp_bridge.py` | stdio Server、动态工具发现和 LangChain Tool 桥接已存在 |
| 会话持久化 | `app/cli.py` | 使用 LangGraph SQLite checkpoint，按 `thread_id` 续聊 |

当前最重要的缺口不是再加一个框架，而是：**把 Agent 的核心循环从高层黑盒逐步落到自己的代码里，并让已有工具、RAG、MCP、SQLite 都接到这条明确主线上。**

## 3. 优先级总表

优先级按“所有成熟 Agent 都需要”到“特定产品才需要”排列。每一阶段都先写代码、跑真实例子、更新 README，再独立提交一次 Git commit。

### M1：手写 Agent 主循环（最高优先级）

目标：用自己的 `app/manual_loop.py` 表达最小 ReAct 闭环，暂时保留 `create_agent` 版本作为对照，不再让它成为唯一实现。

必须覆盖：

- 组装 system/user/messages/tools 请求；
- 调模型并读取文本或 `tool_calls`；
- JSON 参数解析与基础 Schema 校验；
- 通过工具名找到 Python handler；
- 执行工具、生成 tool message、回灌下一轮；
- `max_iterations`、未知工具、坏参数、工具异常和空响应的安全出口；
- 保存每轮 assistant/tool 消息，确保重启后不会丢失上下文。

**完成标准**：不用框架自动循环，也能完成“读文件 → 模型解释 → 最终回答”；每个状态变化都能在代码和日志中定位。

### M2：工具注册表与统一执行器

目标：把“模型可见 Schema”和“程序真实函数”放进一个清晰的 Registry，形成可扩展工具系统。

- `ToolSpec`：名称、描述、JSON Schema、handler、风险级别、是否需要审批；
- 统一 `dispatch()`，同步/异步 handler 都走同一出口；
- 输入校验、超时、异常归一化、结果大小限制；
- 工具白名单与 toolset；
- 每个工具记录调用 ID、参数摘要、结果摘要和副作用类型。

**完成标准**：新增一个工具只需注册 Schema 和函数，不需要修改主循环中的一堆 `if/elif`。

### M3：安全执行边界与人工确认

目标：让 Agent “能做事，但不能随便做事”。

- workspace 路径解析、路径逃逸拦截和文件类型/大小限制；
- 只读工具与写入工具分级；
- 写入、外部请求、删除、命令执行等高风险动作进入 approval gate；
- 人工批准、拒绝、修改参数、恢复执行；
- 审计记录：谁请求、模型给了什么参数、谁批准、实际执行了什么。

**完成标准**：能现场演示一次“模型想写文件 → 系统暂停 → 用户批准 → 执行并记录”的完整过程。

### M4：可靠性基础设施

目标：让 Agent 在真实网络和工具故障下仍然可解释、可恢复。

- 每轮和每个工具设置超时、取消和最大重试次数；
- 区分可重试错误、业务拒绝、参数错误和系统错误；
- 幂等键和重复调用保护；
- 持久化顺序：先保存意图，再执行副作用，再保存结果；
- 流式输出、事件类型和中途停止；
- 上下文预算、消息裁剪和摘要压缩。

**完成标准**：模拟超时、坏 JSON、工具抛异常、进程中断四种情况，系统都能给出确定的状态，而不是静默丢失。

### M5：RAG 产品化

目标：把当前教学型 SQLite 全量扫描 RAG，升级为可解释、可扩展的检索组件。

- 独立 ingestion 流程：文件发现、解析、切块、哈希、增量更新；
- 向量库或 PostgreSQL + pgvector / Qdrant 的 Top-K 召回；
- 元数据过滤：来源、时间、权限、文档类型；
- 召回结果保留 source、chunk、分数和版本；
- 证据注入与“无证据不编造”分支；
- 先保留 SQLite 适配器，便于本地学习和迁移对照。

**完成标准**：知识库扩大时不会把所有向量一次性拉入内存；回答能指出依据来自哪个文档片段。

### M6：MCP 与外部工具生态

目标：把 MCP 作为标准化外部工具接入层，而不是把 MCP 当作 Agent 本身。

- Server 配置与生命周期管理；
- 启动时发现 tools/resources/prompts；
- Schema 转换和命名冲突处理；
- stdio 与 HTTP/SSE 等传输的隔离；
- MCP 工具的权限、超时、结果大小和审计；
- MCP Server 崩溃后的重连或安全失败。

**完成标准**：可以替换一个 MCP Server 而不改 Agent 主循环；模型只看到被授权的工具。

### M7：会话、长期记忆与上下文管理

目标：区分“当前对话”“任务状态”“长期记忆”，避免把所有历史无限塞进 prompt。

- `thread_id` 会话恢复；
- 用户偏好和稳定事实的显式写入/删除；
- 记忆检索、冲突和过期策略；
- 消息摘要、最近消息窗口和 token 预算；
- 敏感信息脱敏与记忆权限。

**完成标准**：解释一条信息为什么进入短期上下文、为什么进入长期记忆，以及如何删除它。

### M8：服务化与可观测性

目标：让项目从 CLI 作品变成可展示的应用服务。

- FastAPI 接口、SSE/WebSocket 流式输出；
- 请求 ID、会话 ID、工具调用 ID；
- 结构化日志和事件流；
- 模型耗时、token、工具耗时、错误类型和重试次数；
- 简单 Web UI 或前端接入；
- Docker/Compose 一键启动，`.env` 与数据卷分离。

**完成标准**：面试现场可以启动服务，展示一条请求从模型到工具再回模型的完整 trace。

### M9：规划、并行与子 Agent（后置能力）

目标：只在单 Agent 主循环稳定后增加复杂编排。

- 明确任务分解与计划状态；
- 只读任务并行，写任务顺序屏障；
- 子 Agent 的输入/输出契约和隔离 workspace；
- 失败子任务重试、取消和汇总；
- 何时用工作流，何时用自由 Agent，写成可解释的决策规则。

**完成标准**：不靠“多 Agent 更高级”的口号，能说明并行带来的收益、隔离成本和失败处理。

### M10：评测与回归（最后再做）

这一阶段现在不做，也不加入当前阅读主线。等 M1–M9 的产品能力稳定后，再建立：

- 任务样本、工具调用轨迹和人工标注；
- 检索召回、工具选择、最终回答和安全拒答指标；
- 线上 trace 到离线样本的回放；
- 版本回归和成本/延迟对比。

评测的前提是“被测产品已经有清晰行为”。在主循环、权限、RAG 和服务化尚未完成时，先建 eval 框架只会增加阅读和维护成本。

## 4. 每阶段的固定交付方式

每个 M 阶段都遵循同一个小闭环：

```text
设计一页说明 → 实现一个最小切片 → 真实运行一个案例
      ↓                  ↓                 ↓
更新 README       保留安全测试        独立 Git commit
```

每次提交只解决一个能力，不把“重构、换数据库、加 UI、加多 Agent”混在同一个 commit。这样面试时可以按 commit 讲清楚：问题是什么、选择是什么、代码改在哪里、验证结果是什么。

## 5. 第一阶段 M1 的文件计划

第一步只实现手写循环，不提前做评测：

```text
app/
├─ manual_loop.py       # 手写模型 → tool_call → handler → tool_result 循环
├─ tool_registry.py     # 从现有工具定义抽出统一 Registry（M2 可继续增强）
├─ workspace_tools.py    # 保留现有安全文件工具
├─ rag_tools.py          # 暂保持现有能力，先作为普通工具接入
├─ mcp_bridge.py         # 暂保持现有能力，先作为普通工具接入
└─ agent.py              # 保留 create_agent 对照入口
```

M1 不复制 Hermes 的复杂 runtime，也不追求一次覆盖所有 Agent 产品特性；只把 common_agent 自己需要的最小主循环写清楚。

## 6. 公开设计依据

以下资料用于确定“共性能力”的优先级，不把任何一家产品的专有实现照搬进来：

- OpenAI Agents SDK：Agent/Runner 统一管理 turns、tools、guardrails、handoffs 和 sessions；官方文档还把 tracing 作为运行记录能力。<https://openai.github.io/openai-agents-python/agents/> <https://openai.github.io/openai-agents-python/tracing/>
- LangGraph：重点提供 durable execution、streaming、human-in-the-loop、persistence 和 memory。<https://langchain-ai.github.io/langgraph/reference/>
- MCP：协议把 tools、resources、prompts 分成不同能力面，工具是模型可控制的动作，资源是上下文数据。<https://modelcontextprotocol.io/specification/2025-06-18/server>
- OpenAI Agents guardrails：工具调用可以在执行前后校验并阻断，说明安全边界应该贴近工具执行点，而不是只写在 system prompt。<https://openai.github.io/openai-agents-python/guardrails/>
- AutoGen：官方文档把 memory、teams、tools、MCP 和 streaming 作为 AgentChat 的组成能力；它适合作为后续多 Agent 对照，不作为当前 M1 的实现起点。<https://microsoft.github.io/autogen/dev/user-guide/agentchat-user-guide/memory.html>

## 7. 当前明确不做的事情

- 不在 M1 前建立 eval 数据集或评分框架。
- 不为了“像某个开源 Agent”复制复杂插件系统、终端沙箱或多 Agent runtime。
- M3 重新为安全边界补最小测试；在此之前不把测试框架混进 M1 的源码阅读主线。
- 不把 RAG、MCP、记忆和多 Agent 混成一个巨型重构；按 M1–M9 逐段接入。
