# 2026-09-11 查询 PyPI 后锁定的当前版本。
# 锁版本的目的：两台电脑装出来的环境一致，避免“昨天能跑、今天不能跑”。
langchain==1.4.0
langchain-openai==1.6.2
langgraph==1.2.11
langgraph-checkpoint-sqlite==3.1.1
python-dotenv==1.2.3

# 官方 MCP Python SDK。当前 LangChain MCP 适配器 0.3.2 仍要求 mcp<2，
# 因此本项目直接使用最新官方 SDK 2.2.0，避免为了适配器退回旧协议实现。
mcp==2.2.0
